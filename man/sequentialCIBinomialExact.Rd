% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binomialExactCI.R
\name{sequentialCIBinomialExact}
\alias{sequentialCIBinomialExact}
\title{Exact sequential confidence intervals for vaccine or prevention efficacy}
\usage{
sequentialCIBinomialExact(
  gsD,
  n.I = NULL,
  x = NULL,
  conf.level = 0.95,
  tol = 1e-08,
  maxiter = 100
)
}
\arguments{
\item{gsD}{A `gsSurv` object with non-binding `test.type` 1, 4, 6, or 8.}

\item{n.I}{Increasing integer total event counts at completed analyses. If
`NULL`, planned integer event counts from `toInteger(gsD)` are used.}

\item{x}{Integer experimental-arm event counts at the analyses in `n.I`.}

\item{conf.level}{Two-sided confidence level.}

\item{tol}{Absolute tolerance for bisection on the conditional binomial
event-probability scale.}

\item{maxiter}{Maximum bisection iterations for each confidence limit.}
}
\value{
A data frame with one row per completed analysis containing the
  observed counts, efficacy estimate, sequential confidence limits,
  confidence level, and one-sided tail level.
}
\description{
Forms a sequential confidence interval after each completed analysis by
intersecting the exact repeated confidence intervals through that analysis.
}
\details{
The interval is the inversion of [sequentialPValueBinomialExact()] applied
in both directions: a candidate efficacy remains in the confidence set only
when neither one-sided repeated test has rejected it at any completed
analysis. Thus the sequential interval through analysis `j` is the
intersection of repeated intervals 1 through `j`.
}
\examples{
design <- gsSurv(
  k = 3, test.type = 4, timing = c(.45, .7), ratio = 3,
  hr = .3, hr0 = .7
)
counts <- toBinomialExact(design)$n.I
\donttest{
sequentialCIBinomialExact(design, counts, x = c(12, 23, 38))
}
}
\seealso{
[ciBinomialExact()], [repeatedCIBinomialExact()],
  [sequentialPValueBinomialExact()]
}
