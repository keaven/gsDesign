% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binomialPowerTable.R
\name{binomialPowerTable}
\alias{binomialPowerTable}
\alias{simPower}
\title{Power Table for Binomial Tests}
\usage{
binomialPowerTable(
  pC = c(0.8, 0.9, 0.95),
  delta = seq(-0.05, 0.05, 0.025),
  n = 70,
  ratio = 1,
  alpha = 0.025,
  delta0 = -0.2,
  scale = "Difference",
  failureEndpoint = TRUE,
  simulation = FALSE,
  exact = FALSE,
  nsim = 1e+06
)

simPower(
  longTable,
  n = 70,
  ratio = 1,
  alpha = 0.025,
  delta0 = -0.2,
  scale = "Difference",
  failureEndpoint = TRUE,
  nsim = 10000
)
}
\arguments{
\item{pC}{Vector of control group response rates}

\item{delta}{Vector of treatment effects (differences in response rates)}

\item{n}{Total sample size}

\item{ratio}{Ratio of experimental to control sample size}

\item{alpha}{Type I error rate}

\item{delta0}{Non-inferiority margin}

\item{scale}{Scale for the test ("Difference", "RR", or "OR")}

\item{failureEndpoint}{Logical indicating if the endpoint is a failure (TRUE) or success (FALSE)}

\item{simulation}{Logical indicating whether to use simulation (TRUE) or analytical (FALSE) power calculation}

\item{exact}{Logical indicating whether to use exact binomial test}

\item{nsim}{Number of simulations to run when simulation=TRUE}

\item{longTable}{Data frame containing pC and pE values for simulation}
}
\value{
A data frame containing:
\itemize{
  \item pC: Control group response rate
  \item delta: Treatment effect
  \item pE: Experimental group response rate
  \item Power: Power for the test
  \item simPower: Power from simulation (when simulation=TRUE)
}
}
\description{
Creates a power table for binomial tests with various control group response rates and treatment effects.
The function can compute power either analytically or through simulation.
With large simulations, the function is still fast and can produces exact power values to simulation error.
}
\details{
The function creates a grid of all combinations of control group response rates and treatment effects.
For each combination, it computes the power either analytically using \code{nBinomial} or through
simulation using \code{simBinomial}. When using simulation, the \code{simPower} function is called
internally to perform the simulations.
}
\examples{
# Create a power table with analytical power calculation
power_table <- binomialPowerTable(
  pC = c(0.8, 0.9),
  delta = seq(-0.05, 0.05, 0.025),
  n = 70
)

# Create a power table with simulation
power_table_sim <- binomialPowerTable(
  pC = c(0.8, 0.9),
  delta = seq(-0.05, 0.05, 0.025),
  n = 70,
  simulation = TRUE,
  nsim = 10000
)

}
\seealso{
\code{\link{nBinomial}}, \code{\link{simBinomial}}
}
